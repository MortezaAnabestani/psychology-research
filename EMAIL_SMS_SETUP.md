# راهنمای تنظیمات ایمیل و پیامک (Email & SMS Setup Guide)

این راهنما شامل دستورالعمل‌های کامل برای تنظیم سیستم ارسال ایمیل و پیامک در پروژه است.

---

## فهرست مطالب
1. [تنظیمات ایمیل (Email Setup)](#تنظیمات-ایمیل)
2. [تنظیمات پیامک - کاوه‌نگار (SMS Setup)](#تنظیمات-پیامک)
3. [انواع ارسال خودکار و دستی](#انواع-ارسال)
4. [نکات امنیتی](#نکات-امنیتی)
5. [عیب‌یابی مشکلات رایج](#عیب‌یابی)

---

## تنظیمات ایمیل

### 1. پیش‌نیازها
- یک حساب ایمیل فعال (در این پروژه: `mail@mashaayekhi.ir`)
- دسترسی به تنظیمات SMTP هاست یا سرویس ایمیل
- رمز عبور ایمیل یا Application Password

### 2. پیدا کردن تنظیمات SMTP هاست

#### برای هاست‌های ایرانی (مانند هاست شما):

**گام 1: ورود به cPanel**
- وارد پنل کنترل هاست (cPanel) شوید
- به بخش "Email Accounts" یا "حساب‌های ایمیل" بروید

**گام 2: یافتن تنظیمات SMTP**
معمولاً تنظیمات به شکل زیر است:
```
SMTP Server: mail.mashaayekhi.ir (یا mail.yourdomain.com)
Port: 587 (TLS) یا 465 (SSL)
Username: mail@mashaayekhi.ir (آدرس کامل ایمیل)
Password: رمز عبور ایمیل شما
```

**گام 3: بررسی پورت‌های مجاز**
معمولاً هاست‌های ایرانی از پورت‌های زیر پشتیبانی می‌کنند:
- **پورت 587**: TLS/STARTTLS (توصیه می‌شود) ✅
- **پورت 465**: SSL
- **پورت 25**: غیر رمزنگاری شده (توصیه نمی‌شود)

#### برای Gmail:
```env
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
```

**⚠️ نکته مهم برای Gmail:**
- باید از "App Password" استفاده کنید، نه رمز عبور اصلی
- برای دریافت App Password:
  1. به https://myaccount.google.com/security بروید
  2. "2-Step Verification" را فعال کنید
  3. در بخش "App passwords" یک رمز جدید بسازید

#### برای Outlook/Hotmail:
```env
EMAIL_HOST=smtp-mail.outlook.com
EMAIL_PORT=587
EMAIL_USER=your-email@outlook.com
EMAIL_PASSWORD=your-password
```

### 3. تنظیم فایل .env

فایل `.env` را در مسیر `backend/.env` ایجاد کنید:

```env
# Email Settings
EMAIL_USER=mail@mashaayekhi.ir
EMAIL_PASSWORD=your-actual-password-here
EMAIL_HOST=mail.mashaayekhi.ir
EMAIL_PORT=587
```

### 4. تست ارسال ایمیل

بعد از تنظیمات، می‌توانید با استفاده از این دستور سیستم را تست کنید:

```javascript
// در Node.js REPL یا یک فایل تست
const { sendEmail } = require('./src/config/email');

sendEmail(
  'test-recipient@example.com',
  'تست ایمیل',
  '<h1>این یک ایمیل تست است</h1>'
).then(() => {
  console.log('ایمیل با موفقیت ارسال شد!');
}).catch(err => {
  console.error('خطا در ارسال:', err);
});
```

---

## تنظیمات پیامک

### 1. ثبت‌نام و دریافت API Key از کاوه‌نگار

**گام 1: ثبت‌نام**
1. به آدرس https://panel.kavenegar.com بروید
2. روی "ثبت نام" کلیک کنید
3. اطلاعات خود را وارد کنید و حساب کاربری بسازید

**گام 2: احراز هویت**
1. برای استفاده از سرویس، باید احراز هویت شوید
2. مدارک لازم را آپلود کنید
3. منتظر تایید بمانید (معمولاً 1-2 روز کاری)

**گام 3: دریافت API Key**
1. وارد پنل کاوه‌نگار شوید
2. از منوی سمت راست روی "تنظیمات" کلیک کنید
3. در قسمت "API Key" کلید خود را کپی کنید
4. این کلید را در جای امنی ذخیره کنید

### 2. انتخاب سرویس مناسب

کاوه‌نگار سرویس‌های مختلفی ارائه می‌دهد:

#### ✅ سرویس‌های مورد نیاز این پروژه:

**1. سرویس پایه (Simple Send):**
- برای ارسال پیامک‌های ساده و متنی
- **استفاده در پروژه:**
  - ارسال نوتیفیکیشن‌های تمرین
  - پیامک‌های انگیزشی
  - پیامک گروهی
- **هزینه:** حدود 30-50 ریال به ازای هر پیامک
- **پیشنهاد:** شروع با بسته 1000 پیامکی (حدود 40,000 تومان)

**2. سرویس لوکاپ (Lookup/Template):**
- برای ارسال پیامک‌های الگویی
- کد در حال حاضر از این قابلیت پشتیبانی می‌کند
- **مزایا:**
  - سرعت بیشتر
  - احتمال کمتر برای فیلتر شدن
- **هزینه:** مشابه سرویس پایه

#### ❌ سرویس‌های که نیاز ندارید:

- **OTP (کد تایید):** برای پروژه شما لازم نیست
- **خطوط اختصاصی:** در ابتدا نیاز نیست (می‌توانید از شماره عمومی استفاده کنید)

### 3. تنظیم فایل .env

```env
# Kavenegar Settings
KAVENEGAR_API_KEY=your-api-key-from-panel
KAVENEGAR_SENDER=10008663
```

**نکته:** شماره `10008663` یک شماره عمومی کاوه‌نگار است. اگر خط اختصاصی دارید، از آن استفاده کنید.

### 4. تست ارسال پیامک

```javascript
const { SMSService } = require('./src/services/smsService');

// تست ارسال پیامک ساده
SMSService.sendSMS('09123456789', 'این یک پیامک تست است')
  .then(result => {
    console.log('نتیجه:', result);
  });

// تست ارسال با قالب (Template)
SMSService.sendTemplateSMS(
  '09123456789',
  'template-name',
  { token: 'مقدار تست' }
).then(result => {
  console.log('نتیجه:', result);
});
```

### 5. برآورد هزینه

برای یک پروژه تحقیقاتی با 50 مشارکت‌کننده:

```
تعداد کاربران: 50 نفر
پیامک‌های روزانه به ازای هر نفر: 2-3 عدد
مدت پروژه: فرض کنیم 30 روز

محاسبه:
50 کاربر × 3 پیامک × 30 روز = 4500 پیامک
هزینه تقریبی: 4500 × 40 ریال = 180,000 ریال (حدود 180 هزار تومان)

پیشنهاد: شارژ اولیه 5000 پیامکی (حدود 200 هزار تومان)
```

---

## انواع ارسال

### ارسال خودکار (Automatic Sending)

#### 1. نوتیفیکیشن‌های تمرین
**فایل:** `backend/src/services/notificationService.ts`

سیستم به صورت خودکار برای هر تمرین نوتیفیکیشن‌هایی ارسال می‌کند:
- **Push Notification:** اعلان در مرورگر
- **Email:** ایمیل یادآوری با لینک تمرین
- **SMS:** پیامک یادآوری

**زمان‌بندی:**
- بر اساس تنظیمات هر تمرین (Template)
- می‌تواند ثابت، تصادفی یا بر اساس زمان انتخابی کاربر باشد

#### 2. یادآوری تمرین‌های ناتمام
**فایل:** `backend/src/services/cronJobs.ts` (خطوط 36-67)

- **زمان:** هر روز ساعت 20:00 (8 شب)
- **شرط:** تمرین‌هایی که بیش از 24 ساعت است دست نخورده‌اند
- **ارسال:** فقط ایمیل

#### 3. پیامک‌های انگیزشی
**فایل:** `backend/src/services/cronJobs.ts` (خطوط 69-148)

- **زمان:** دو بار در روز به صورت تصادفی
  - موج اول: 9 صبح تا 3 بعدازظهر
  - موج دوم: 3 بعدازظهر تا 9 شب
- **محتوا:** متن انگیزشی تعریف شده در Template
- **ارسال:** فقط SMS

### ارسال دستی (Manual Sending)

#### پنل ارسال پیام گروهی
**فایل:** `backend/src/routes/admin.ts` (خطوط 735-822)

**قابلیت‌ها:**
1. **انتخاب مخاطبان:**
   - همه کاربران فعال
   - فقط گروه کنترل
   - فقط گروه مداخله
   - انتخاب فردی کاربران خاص

2. **نوع ارسال:**
   - فقط ایمیل
   - فقط پیامک
   - هر دو (ایمیل + پیامک)

3. **محتوا:**
   - موضوع (Subject) - فقط برای ایمیل
   - متن پیام

**مسیر در فرانت‌اند:**
`/admin/bulk-message`

---

## نکات امنیتی

### 1. حفاظت از فایل .env

**⚠️ بسیار مهم:**
- فایل `.env` را **هرگز** در Git آپلود نکنید
- از `.gitignore` برای حذف `.env` استفاده شده است
- فایل `.env.example` یک نمونه بدون اطلاعات حساس است

### 2. رمزهای عبور قوی

```bash
# تولید یک JWT Secret قوی:
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
```

### 3. تولید کلیدهای VAPID

برای Web Push Notifications:

```bash
# نصب ابزار
npm install -g web-push

# تولید کلیدها
web-push generate-vapid-keys
```

خروجی را در `.env` قرار دهید:
```env
VAPID_EMAIL=mail@mashaayekhi.ir
VAPID_PUBLIC_KEY=کلید-عمومی-تولید-شده
VAPID_PRIVATE_KEY=کلید-خصوصی-تولید-شده
```

### 4. محدود کردن دسترسی

در سرور، مطمئن شوید:
```bash
chmod 600 backend/.env
```

---

## عیب‌یابی

### مشکلات رایج ایمیل

#### 1. خطای "Authentication failed"
**علت:** نام کاربری یا رمز عبور اشتباه است

**راه حل:**
- مطمئن شوید `EMAIL_USER` و `EMAIL_PASSWORD` صحیح هستند
- برای Gmail از App Password استفاده کنید
- تنظیمات SMTP هاست را بررسی کنید

#### 2. خطای "Connection timeout"
**علت:** پورت یا هاست اشتباه است یا فایروال مسدود کرده

**راه حل:**
```env
# امتحان کنید پورت را تغییر دهید:
EMAIL_PORT=465  # به جای 587

# یا در کد email.ts secure را true کنید:
secure: true  // برای پورت 465
```

#### 3. ایمیل به اسپم می‌رود
**علت:** عدم تنظیمات SPF/DKIM/DMARC

**راه حل:**
- با هاست خود تماس بگیرید
- از آنها بخواهید SPF و DKIM را تنظیم کنند
- محتوای ایمیل را بهینه کنید (کمتر از لینک استفاده کنید)

### مشکلات رایج SMS

#### 1. خطای "API Key is not configured"
**راه حل:**
```env
# مطمئن شوید در .env تنظیم شده:
KAVENEGAR_API_KEY=your-actual-api-key
```

#### 2. خطای 401 (Unauthorized)
**علت:** API Key اشتباه است

**راه حل:**
- API Key را از پنل کاوه‌نگار دوباره کپی کنید
- مطمئن شوید فاصله یا کاراکتر اضافی ندارد

#### 3. خطای 422 (Unprocessable Entity)
**علت:** فرمت شماره تلفن اشتباه است

**راه حل:**
کد به صورت خودکار فرمت را اصلاح می‌کند، اما اگر مشکل داشتید:
```javascript
// فرمت صحیح:
"09123456789"  // ✅
"9123456789"   // ✅ (کد خودکار 0 را اضافه می‌کند)
"989123456789" // ✅

// فرمت‌های اشتباه:
"+989123456789" // ❌
"09-12-345-6789" // ❌
```

#### 4. پیامک ارسال نمی‌شود ولی خطا ندارد
**علت احتمالی:**
- اعتبار کاوه‌نگار تمام شده
- شماره در لیست سیاه است

**راه حل:**
1. وارد پنل کاوه‌نگار شوید
2. موجودی خود را چک کنید
3. لاگ‌های ارسال را بررسی کنید

### بررسی لاگ‌ها

برای دیدن لاگ‌های دقیق:

```bash
# در محیط development:
npm run dev

# در محیط production:
pm2 logs
# یا
tail -f /path/to/logs/app.log
```

---

## تماس با پشتیبانی

### کاوه‌نگار:
- **وب‌سایت:** https://kavenegar.com
- **پنل:** https://panel.kavenegar.com
- **مستندات:** https://kavenegar.com/rest.html
- **پشتیبانی:** support@kavenegar.com

### سوالات پروژه:
برای مشکلات مرتبط با کد، لاگ‌های خطا را بررسی کنید یا با توسعه‌دهنده تماس بگیرید.

---

## چک‌لیست راه‌اندازی

- [ ] فایل `.env` در مسیر `backend/.env` ایجاد شد
- [ ] تنظیمات ایمیل وارد شد (`EMAIL_USER`, `EMAIL_PASSWORD`, `EMAIL_HOST`, `EMAIL_PORT`)
- [ ] تست ارسال ایمیل انجام شد و موفق بود
- [ ] حساب کاوه‌نگار ساخته و احراز هویت شد
- [ ] API Key از کاوه‌نگار دریافت شد
- [ ] `KAVENEGAR_API_KEY` در `.env` قرار گرفت
- [ ] اعتبار کاوه‌نگار شارژ شد
- [ ] تست ارسال پیامک انجام شد
- [ ] کلیدهای VAPID تولید و در `.env` قرار گرفتند
- [ ] `CLIENT_URL` به آدرس صحیح تنظیم شد
- [ ] سرویس‌های Cron Job فعال هستند
- [ ] دسترسی به فایل `.env` محدود شد (chmod 600)

---

**تاریخ ایجاد:** 2025-12-07
**نسخه:** 1.0
